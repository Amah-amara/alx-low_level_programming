#include <stdio.h>

/**
 * main - program compilation starts here
 * description: the program will print the name of the file as a string literal
 * Return: 0
 */

int main(void)
{
	printf("filename is : %s\n", __FILE__);
	return (0);
}
